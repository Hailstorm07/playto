{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn playto_pay.wsgi --bind 0.0.0.0:8000 --log-file -",
    "restartPolicyMaxRetries": 5
  }
}
